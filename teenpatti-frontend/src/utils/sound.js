/**
 * No-op sound hooks. Drop files in src/assets/sounds/ and wire them in here
 * (e.g. `new Audio('/src/assets/sounds/chip.mp3').play()`) when you have assets.
 * Every call site in the components already calls these — nothing else to change.
 */
export function chipSound() {}
export function cardFlipSound() {}
export function dealSound() {}
export function winSound() {}
